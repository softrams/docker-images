integrations:
  - name: nri-apache
    env:
      METRICS: "true"
      STATUS_URL: {{APACHE_STATUS}}
      REMOTE_MONITORING: true
    interval: {{NRIA_INTERVAL}}
    labels:
      environment: {{ENVIRONMENT}}
      project: {{PROJECT}}
